/**
 * Command to install typescript
 */
export const typeScript =
  'npm install typescript @types/node --save-dev';
/**
 * Command to install linters
 */
export const lintScript =
  'npm install prettier ts-lint  tslint-config-prettier --save-dev';
/**
 * Command to install jest
 */
export const jestScript =
  'npm install jest @types/jest ts-jest --save-dev';
